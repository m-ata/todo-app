/* eslint-disable no-unused-vars */
export enum STATUS {
  COMPLETED = 'completed',
  PENDING = 'pending',
  OVERDUE = 'overdue',
}

export enum HTTP_STATUS_CODES {
  CREATED = 201,
  OK = 200,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
  INTERNAL_ERROR = 500,
}
